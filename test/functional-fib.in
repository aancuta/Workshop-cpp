+{
    fib = {
        prev = 0;
        curr = 1;

        i = 0;

        adder = {
            aux = prev + curr;
            prev = curr;
            print curr = aux;
            if (n - i) {
                i = i + 1;
                adder
            }
            else curr
        };
    };
    n = 25;
    fib
}
